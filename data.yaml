# Define the paths to the training, validation, and test data
train: \train  # Path to training images
val: \validation     # Path to validation images
test: \test    # Path to test images (optional)

# Define the names and indices of the classes (labels)
names:
  0: "cws"

# Define the number of classes
nc: 1  # Number of classes (update this according to your dataset)
